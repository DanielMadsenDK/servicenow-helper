# Test Environment Variables
# These are the default credentials used for Playwright testing
# Override these in .env.test.local for local testing if needed

# Test Authentication - these should match what the server expects
# In CI/CD environments, set these as environment variables
# For local development, .env.local will override these if present
TEST_AUTH_USERNAME=admin
TEST_AUTH_PASSWORD=password123

# Test JWT Secret (can be any value for testing)
TEST_JWT_SECRET=test-jwt-secret-for-playwright-testing-only

# Required environment variables for the app to function during tests
# These are dummy values since the API calls are mocked
JWT_SECRET=test-jwt-secret-for-playwright-testing-only
AUTH_USERNAME=admin
AUTH_PASSWORD=password123
N8N_WEBHOOK_URL=http://localhost:5678/webhook/test
N8N_WEBHOOK_URL_RESPONSE=http://localhost:5678/webhook/test-response
N8N_API_KEY=test-api-key